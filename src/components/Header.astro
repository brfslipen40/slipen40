---
const routeDefinitions: {
  link: string;
  name: string;
}[] = [
  {
    link: "/",
    name: "BRF Slipen 40",
  },
  {
    link: "/info",
    name: "Information",
  },
  {
    link: "/contact",
    name: "Kontakt",
  },
];
---

<header class="flex gap-2 bg-teal-300 p-4">
  {routeDefinitions.map((route) => <a href={route.link}>{route.name}</a>)}
</header>
